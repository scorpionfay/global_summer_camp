import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Schools data lives one level up from the web/ folder
  // We copy it into src/data at build time via the data loader
};

export default nextConfig;
